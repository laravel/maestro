name: Build and Create PRs

on:
  push:
    branches:
      - main

jobs:
  build-livewire:
    name: Build Livewire Kit
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Maestro
        uses: actions/checkout@v5

      - name: Setup PHP
        uses: shivammathur/setup-php@v2
        with:
          php-version: '8.4'
          extensions: mbstring, xml, ctype, json, curl

      - name: Install Composer Dependencies
        working-directory: orchestrator
        run: composer install --no-interaction --prefer-dist

      - name: Build Livewire Kit
        working-directory: orchestrator
        run: php artisan build --kit=Livewire

      - name: Checkout Livewire Starter Kit
        uses: actions/checkout@v5
        with:
          repository: laravel/livewire-starter-kit
          path: livewire-starter-kit
          token: ${{ secrets.STARTER_KIT_TOKEN }}

      - name: Copy Build to Livewire Starter Kit
        run: |
          rm -rf livewire-starter-kit/*
          cp -r build/* livewire-starter-kit/
          cp -r build/.[!.]* livewire-starter-kit/ 2>/dev/null || true

      - name: Check for Changes
        id: changes
        uses: ./.github/actions/check-changes
        with:
          path: livewire-starter-kit

      - name: Install and Lint Livewire Kit
        if: steps.changes.outputs.has_changes == 'true'
        working-directory: livewire-starter-kit
        run: |
          composer install --no-interaction --prefer-dist
          composer lint

      - name: Create Pull Request for Livewire
        if: steps.changes.outputs.has_changes == 'true'
        uses: peter-evans/create-pull-request@v6
        with:
          path: livewire-starter-kit
          token: ${{ secrets.STARTER_KIT_TOKEN }}
          commit-message: 'Update from Maestro'
          title: 'Update from Maestro'
          body: |
            This PR was automatically generated by Maestro.

            Triggered by commit: ${{ github.sha }}
          branch: maestro-update
          base: main

  build-livewire-blank:
    name: Build Livewire Kit (Blank)
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Maestro
        uses: actions/checkout@v5

      - name: Setup PHP
        uses: shivammathur/setup-php@v2
        with:
          php-version: '8.4'
          extensions: mbstring, xml, ctype, json, curl

      - name: Install Composer Dependencies
        working-directory: orchestrator
        run: composer install --no-interaction --prefer-dist

      - name: Build Livewire Kit (Blank)
        working-directory: orchestrator
        run: php artisan build --kit=Livewire --blank

      - name: Checkout Blank Livewire Starter Kit
        uses: actions/checkout@v5
        with:
          repository: laravel/blank-livewire-starter-kit
          path: blank-livewire-starter-kit
          token: ${{ secrets.STARTER_KIT_TOKEN }}

      - name: Copy Build to Blank Livewire Starter Kit
        run: |
          rm -rf blank-livewire-starter-kit/*
          cp -r build/* blank-livewire-starter-kit/
          cp -r build/.[!.]* blank-livewire-starter-kit/ 2>/dev/null || true

      - name: Check for Changes
        id: changes
        uses: ./.github/actions/check-changes
        with:
          path: blank-livewire-starter-kit

      - name: Install and Lint Livewire Kit
        if: steps.changes.outputs.has_changes == 'true'
        working-directory: blank-livewire-starter-kit
        run: |
          composer install --no-interaction --prefer-dist
          composer lint

      - name: Create Pull Request for Livewire (Blank)
        if: steps.changes.outputs.has_changes == 'true'
        uses: peter-evans/create-pull-request@v6
        with:
          path: blank-livewire-starter-kit
          token: ${{ secrets.STARTER_KIT_TOKEN }}
          commit-message: 'Update from Maestro'
          title: 'Update from Maestro'
          body: |
            This PR was automatically generated by Maestro.

            Triggered by commit: ${{ github.sha }}
          branch: maestro-update
          base: main

  build-livewire-workos:
    name: Build Livewire Kit (WorkOS)
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Maestro
        uses: actions/checkout@v5

      - name: Setup PHP
        uses: shivammathur/setup-php@v2
        with:
          php-version: '8.4'
          extensions: mbstring, xml, ctype, json, curl

      - name: Install Composer Dependencies
        working-directory: orchestrator
        run: composer install --no-interaction --prefer-dist

      - name: Build Livewire Kit (WorkOS)
        working-directory: orchestrator
        run: php artisan build --kit=Livewire --workos

      - name: Checkout Livewire Starter Kit
        uses: actions/checkout@v5
        with:
          repository: laravel/livewire-starter-kit
          ref: workos
          path: livewire-starter-kit
          token: ${{ secrets.STARTER_KIT_TOKEN }}

      - name: Copy Build to Livewire Starter Kit
        run: |
          rm -rf livewire-starter-kit/*
          cp -r build/* livewire-starter-kit/
          cp -r build/.[!.]* livewire-starter-kit/ 2>/dev/null || true

      - name: Check for Changes
        id: changes
        uses: ./.github/actions/check-changes
        with:
          path: livewire-starter-kit

      - name: Install and Lint Livewire Kit
        if: steps.changes.outputs.has_changes == 'true'
        working-directory: livewire-starter-kit
        run: |
          composer install --no-interaction --prefer-dist
          composer lint

      - name: Create Pull Request for Livewire (WorkOS)
        if: steps.changes.outputs.has_changes == 'true'
        uses: peter-evans/create-pull-request@v6
        with:
          path: livewire-starter-kit
          token: ${{ secrets.STARTER_KIT_TOKEN }}
          commit-message: 'Update from Maestro (WorkOS)'
          title: 'Update from Maestro (WorkOS)'
          body: |
            This PR was automatically generated by Maestro.

            Triggered by commit: ${{ github.sha }}
          branch: maestro-update-workos
          base: workos

  build-livewire-components:
    name: Build Livewire Kit (Components)
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Maestro
        uses: actions/checkout@v5

      - name: Setup PHP
        uses: shivammathur/setup-php@v2
        with:
          php-version: '8.4'
          extensions: mbstring, xml, ctype, json, curl

      - name: Install Composer Dependencies
        working-directory: orchestrator
        run: composer install --no-interaction --prefer-dist

      - name: Build Livewire Kit (Components)
        working-directory: orchestrator
        run: php artisan build --kit=Livewire --components

      - name: Checkout Livewire Starter Kit
        uses: actions/checkout@v5
        with:
          repository: laravel/livewire-starter-kit
          ref: components
          path: livewire-starter-kit
          token: ${{ secrets.STARTER_KIT_TOKEN }}

      - name: Copy Build to Livewire Starter Kit
        run: |
          rm -rf livewire-starter-kit/*
          cp -r build/* livewire-starter-kit/
          cp -r build/.[!.]* livewire-starter-kit/ 2>/dev/null || true

      - name: Check for Changes
        id: changes
        uses: ./.github/actions/check-changes
        with:
          path: livewire-starter-kit

      - name: Install and Lint Livewire Kit
        if: steps.changes.outputs.has_changes == 'true'
        working-directory: livewire-starter-kit
        run: |
          composer install --no-interaction --prefer-dist
          composer lint

      - name: Create Pull Request for Livewire (Components)
        if: steps.changes.outputs.has_changes == 'true'
        uses: peter-evans/create-pull-request@v6
        with:
          path: livewire-starter-kit
          token: ${{ secrets.STARTER_KIT_TOKEN }}
          commit-message: 'Update from Maestro (Components)'
          title: 'Update from Maestro (Components)'
          body: |
            This PR was automatically generated by Maestro.

            Triggered by commit: ${{ github.sha }}
          branch: maestro-update-components
          base: components

  build-react:
    name: Build React Kit
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Maestro
        uses: actions/checkout@v5

      - name: Setup PHP
        uses: shivammathur/setup-php@v2
        with:
          php-version: '8.4'
          extensions: mbstring, xml, ctype, json, curl

      - name: Install Composer Dependencies
        working-directory: orchestrator
        run: composer install --no-interaction --prefer-dist

      - name: Build React Kit
        working-directory: orchestrator
        run: php artisan build --kit=React

      - name: Checkout React Starter Kit
        uses: actions/checkout@v5
        with:
          repository: laravel/react-starter-kit
          path: react-starter-kit
          token: ${{ secrets.STARTER_KIT_TOKEN }}

      - name: Copy Build to React Starter Kit
        run: |
          rm -rf react-starter-kit/*
          cp -r build/* react-starter-kit/
          cp -r build/.[!.]* react-starter-kit/ 2>/dev/null || true

      - name: Check for Changes
        id: changes
        uses: ./.github/actions/check-changes
        with:
          path: react-starter-kit

      - name: Setup Node.js
        if: steps.changes.outputs.has_changes == 'true'
        uses: actions/setup-node@v4
        with:
          node-version: '22'

      - name: Install and Lint React Kit
        if: steps.changes.outputs.has_changes == 'true'
        working-directory: react-starter-kit
        run: |
          composer install --no-interaction --prefer-dist
          composer lint
          npm install
          npm run format
          npm run lint

      - name: Create Pull Request for React
        if: steps.changes.outputs.has_changes == 'true'
        uses: peter-evans/create-pull-request@v6
        with:
          path: react-starter-kit
          token: ${{ secrets.STARTER_KIT_TOKEN }}
          commit-message: 'Update from Maestro'
          title: 'Update from Maestro'
          body: |
            This PR was automatically generated by Maestro.

            Triggered by commit: ${{ github.sha }}
          branch: maestro-update
          base: main

  build-react-blank:
    name: Build React Kit (Blank)
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Maestro
        uses: actions/checkout@v5

      - name: Setup PHP
        uses: shivammathur/setup-php@v2
        with:
          php-version: '8.4'
          extensions: mbstring, xml, ctype, json, curl

      - name: Install Composer Dependencies
        working-directory: orchestrator
        run: composer install --no-interaction --prefer-dist

      - name: Build React Kit (Blank)
        working-directory: orchestrator
        run: php artisan build --kit=React --blank

      - name: Checkout Blank React Starter Kit
        uses: actions/checkout@v5
        with:
          repository: laravel/blank-react-starter-kit
          path: blank-react-starter-kit
          token: ${{ secrets.STARTER_KIT_TOKEN }}

      - name: Copy Build to Blank React Starter Kit
        run: |
          rm -rf blank-react-starter-kit/*
          cp -r build/* blank-react-starter-kit/
          cp -r build/.[!.]* blank-react-starter-kit/ 2>/dev/null || true

      - name: Check for Changes
        id: changes
        uses: ./.github/actions/check-changes
        with:
          path: blank-react-starter-kit

      - name: Setup Node.js
        if: steps.changes.outputs.has_changes == 'true'
        uses: actions/setup-node@v4
        with:
          node-version: '22'

      - name: Install and Lint React Kit
        if: steps.changes.outputs.has_changes == 'true'
        working-directory: blank-react-starter-kit
        run: |
          composer install --no-interaction --prefer-dist
          composer lint
          npm install
          npm run format
          npm run lint

      - name: Create Pull Request for React (Blank)
        if: steps.changes.outputs.has_changes == 'true'
        uses: peter-evans/create-pull-request@v6
        with:
          path: blank-react-starter-kit
          token: ${{ secrets.STARTER_KIT_TOKEN }}
          commit-message: 'Update from Maestro'
          title: 'Update from Maestro'
          body: |
            This PR was automatically generated by Maestro.

            Triggered by commit: ${{ github.sha }}
          branch: maestro-update
          base: main

  build-react-workos:
    name: Build React Kit (WorkOS)
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Maestro
        uses: actions/checkout@v5

      - name: Setup PHP
        uses: shivammathur/setup-php@v2
        with:
          php-version: '8.4'
          extensions: mbstring, xml, ctype, json, curl

      - name: Install Composer Dependencies
        working-directory: orchestrator
        run: composer install --no-interaction --prefer-dist

      - name: Build React Kit (WorkOS)
        working-directory: orchestrator
        run: php artisan build --kit=React --workos

      - name: Checkout React Starter Kit
        uses: actions/checkout@v5
        with:
          repository: laravel/react-starter-kit
          ref: workos
          path: react-starter-kit
          token: ${{ secrets.STARTER_KIT_TOKEN }}

      - name: Copy Build to React Starter Kit
        run: |
          rm -rf react-starter-kit/*
          cp -r build/* react-starter-kit/
          cp -r build/.[!.]* react-starter-kit/ 2>/dev/null || true

      - name: Check for Changes
        id: changes
        uses: ./.github/actions/check-changes
        with:
          path: react-starter-kit

      - name: Setup Node.js
        if: steps.changes.outputs.has_changes == 'true'
        uses: actions/setup-node@v4
        with:
          node-version: '22'

      - name: Install and Lint React Kit
        if: steps.changes.outputs.has_changes == 'true'
        working-directory: react-starter-kit
        run: |
          composer install --no-interaction --prefer-dist
          composer lint
          npm install
          npm run format
          npm run lint

      - name: Create Pull Request for React (WorkOS)
        if: steps.changes.outputs.has_changes == 'true'
        uses: peter-evans/create-pull-request@v6
        with:
          path: react-starter-kit
          token: ${{ secrets.STARTER_KIT_TOKEN }}
          commit-message: 'Update from Maestro (WorkOS)'
          title: 'Update from Maestro (WorkOS)'
          body: |
            This PR was automatically generated by Maestro.

            Triggered by commit: ${{ github.sha }}
          branch: maestro-update-workos
          base: workos

  build-vue:
    name: Build Vue Kit
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Maestro
        uses: actions/checkout@v5

      - name: Setup PHP
        uses: shivammathur/setup-php@v2
        with:
          php-version: '8.4'
          extensions: mbstring, xml, ctype, json, curl

      - name: Install Composer Dependencies
        working-directory: orchestrator
        run: composer install --no-interaction --prefer-dist

      - name: Build Vue Kit
        working-directory: orchestrator
        run: php artisan build --kit=Vue

      - name: Checkout Vue Starter Kit
        uses: actions/checkout@v5
        with:
          repository: laravel/vue-starter-kit
          path: vue-starter-kit
          token: ${{ secrets.STARTER_KIT_TOKEN }}

      - name: Copy Build to Vue Starter Kit
        run: |
          rm -rf vue-starter-kit/*
          cp -r build/* vue-starter-kit/
          cp -r build/.[!.]* vue-starter-kit/ 2>/dev/null || true

      - name: Check for Changes
        id: changes
        uses: ./.github/actions/check-changes
        with:
          path: vue-starter-kit

      - name: Setup Node.js
        if: steps.changes.outputs.has_changes == 'true'
        uses: actions/setup-node@v4
        with:
          node-version: '22'

      - name: Install and Lint Vue Kit
        if: steps.changes.outputs.has_changes == 'true'
        working-directory: vue-starter-kit
        run: |
          composer install --no-interaction --prefer-dist
          composer lint
          npm install
          npm run format
          npm run lint

      - name: Create Pull Request for Vue
        if: steps.changes.outputs.has_changes == 'true'
        uses: peter-evans/create-pull-request@v6
        with:
          path: vue-starter-kit
          token: ${{ secrets.STARTER_KIT_TOKEN }}
          commit-message: 'Update from Maestro'
          title: 'Update from Maestro'
          body: |
            This PR was automatically generated by Maestro.

            Triggered by commit: ${{ github.sha }}
          branch: maestro-update
          base: main

  build-vue-blank:
    name: Build Vue Kit (Blank)
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Maestro
        uses: actions/checkout@v5

      - name: Setup PHP
        uses: shivammathur/setup-php@v2
        with:
          php-version: '8.4'
          extensions: mbstring, xml, ctype, json, curl

      - name: Install Composer Dependencies
        working-directory: orchestrator
        run: composer install --no-interaction --prefer-dist

      - name: Build Vue Kit (Blank)
        working-directory: orchestrator
        run: php artisan build --kit=Vue --blank

      - name: Checkout Blank Vue Starter Kit
        uses: actions/checkout@v5
        with:
          repository: laravel/blank-vue-starter-kit
          path: blank-vue-starter-kit
          token: ${{ secrets.STARTER_KIT_TOKEN }}

      - name: Copy Build to Blank Vue Starter Kit
        run: |
          rm -rf blank-vue-starter-kit/*
          cp -r build/* blank-vue-starter-kit/
          cp -r build/.[!.]* blank-vue-starter-kit/ 2>/dev/null || true

      - name: Check for Changes
        id: changes
        uses: ./.github/actions/check-changes
        with:
          path: blank-vue-starter-kit

      - name: Setup Node.js
        if: steps.changes.outputs.has_changes == 'true'
        uses: actions/setup-node@v4
        with:
          node-version: '22'

      - name: Install and Lint Vue Kit
        if: steps.changes.outputs.has_changes == 'true'
        working-directory: blank-vue-starter-kit
        run: |
          composer install --no-interaction --prefer-dist
          composer lint
          npm install
          npm run format
          npm run lint

      - name: Create Pull Request for Vue (Blank)
        if: steps.changes.outputs.has_changes == 'true'
        uses: peter-evans/create-pull-request@v6
        with:
          path: blank-vue-starter-kit
          token: ${{ secrets.STARTER_KIT_TOKEN }}
          commit-message: 'Update from Maestro'
          title: 'Update from Maestro'
          body: |
            This PR was automatically generated by Maestro.

            Triggered by commit: ${{ github.sha }}
          branch: maestro-update
          base: main

  build-vue-workos:
    name: Build Vue Kit (WorkOS)
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Maestro
        uses: actions/checkout@v5

      - name: Setup PHP
        uses: shivammathur/setup-php@v2
        with:
          php-version: '8.4'
          extensions: mbstring, xml, ctype, json, curl

      - name: Install Composer Dependencies
        working-directory: orchestrator
        run: composer install --no-interaction --prefer-dist

      - name: Build Vue Kit (WorkOS)
        working-directory: orchestrator
        run: php artisan build --kit=Vue --workos

      - name: Checkout Vue Starter Kit
        uses: actions/checkout@v5
        with:
          repository: laravel/vue-starter-kit
          ref: workos
          path: vue-starter-kit
          token: ${{ secrets.STARTER_KIT_TOKEN }}

      - name: Copy Build to Vue Starter Kit
        run: |
          rm -rf vue-starter-kit/*
          cp -r build/* vue-starter-kit/
          cp -r build/.[!.]* vue-starter-kit/ 2>/dev/null || true

      - name: Check for Changes
        id: changes
        uses: ./.github/actions/check-changes
        with:
          path: vue-starter-kit

      - name: Setup Node.js
        if: steps.changes.outputs.has_changes == 'true'
        uses: actions/setup-node@v4
        with:
          node-version: '22'

      - name: Install and Lint Vue Kit
        if: steps.changes.outputs.has_changes == 'true'
        working-directory: vue-starter-kit
        run: |
          composer install --no-interaction --prefer-dist
          composer lint
          npm install
          npm run format
          npm run lint

      - name: Create Pull Request for Vue (WorkOS)
        if: steps.changes.outputs.has_changes == 'true'
        uses: peter-evans/create-pull-request@v6
        with:
          path: vue-starter-kit
          token: ${{ secrets.STARTER_KIT_TOKEN }}
          commit-message: 'Update from Maestro (WorkOS)'
          title: 'Update from Maestro (WorkOS)'
          body: |
            This PR was automatically generated by Maestro.

            Triggered by commit: ${{ github.sha }}
          branch: maestro-update-workos
          base: workos
